<Window x:Class="JournalApp2._0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:JournalApp2._0.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Button Content="Выход" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding CloseApplicationCommand}"/>
            <Button Content="Домашка" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding SwithToSecondWindowComand}"/>
        </StackPanel>
    </Grid>
</Window>
